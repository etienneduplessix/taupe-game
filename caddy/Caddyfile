:8080 {
    # API and WebSockets go to backend
    handle /api* {
        reverse_proxy backend:8000
    }
    handle /ws* {
        reverse_proxy backend:8000
    }

    # Everything else goes to frontend
    handle {
        reverse_proxy frontend:3000
    }
}
